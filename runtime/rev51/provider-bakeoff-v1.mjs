export function compareQualifiedProvidersV1({observations,weights}={}){if(!Array.isArray(observations)||!weights||typeof weights!=="object")throw new Error("BAKEOFF_INPUT_REQUIRED");const rows=observations.map(o=>{let score=0;for(const[k,w]of Object.entries(weights)){if(!Number.isFinite(w)||w<0)throw new Error("BAKEOFF_WEIGHT_INVALID");const v=o[k];if(!Number.isSafeInteger(v)||v<0)throw new Error(`BAKEOFF_METRIC_INVALID:${k}`);score+=v*w}return{PROVIDER_ID:o.PROVIDER_ID,SCORE:score}}).sort((a,b)=>a.SCORE-b.SCORE||a.PROVIDER_ID.localeCompare(b.PROVIDER_ID));return Object.freeze({rows,selectedProviderId:rows[0]?.PROVIDER_ID??null,selectionBasis:"MEASURED_NORMALIZED_METRICS"})}
