export const NORMAL_EVIDENCE_CLASSES=Object.freeze(["LOG","MANIFEST","RECEIPT","CHECKPOINT","MODEL_OUTPUT","REQUIRED_ARTIFACT","DIGEST"]);
export const SNAPSHOT_HOLD_REASONS=Object.freeze(["EMERGENCY_PRESERVATION_HOLD","LEGACY_RESOURCE_EVACUATION"]);
export function evaluatePreservationRequestV1(request){if(!request||typeof request!=="object")throw new Error("PRESERVATION_REQUEST_REQUIRED");if(request.CLASS!=="SNAPSHOT"){if(!NORMAL_EVIDENCE_CLASSES.includes(request.CLASS))return Object.freeze({ok:false,code:"EVIDENCE_CLASS_NOT_ALLOWED"});return Object.freeze({ok:true,code:"NORMAL_EVIDENCE_ALLOWED"})}if(!SNAPSHOT_HOLD_REASONS.includes(request.REASON))return Object.freeze({ok:false,code:"FULL_VM_SNAPSHOT_AS_NORMAL_EVIDENCE_DENIED"});for(const k of["DELETE_AFTER","DELETE_CONDITION","ESTIMATED_COST_MILLIUNITS","CANONICAL_DESTINATION","EXTRACTION_PLAN","OWNER_JOB"])if(request[k]===undefined||request[k]===null||request[k]==="")return Object.freeze({ok:false,code:`SNAPSHOT_${k}_REQUIRED`});if(!Number.isSafeInteger(request.ESTIMATED_COST_MILLIUNITS)||request.ESTIMATED_COST_MILLIUNITS<0)return Object.freeze({ok:false,code:"SNAPSHOT_ESTIMATED_COST_INVALID"});if(!Number.isFinite(Date.parse(request.DELETE_AFTER)))return Object.freeze({ok:false,code:"SNAPSHOT_DELETE_AFTER_INVALID"});return Object.freeze({ok:true,code:"EXCEPTIONAL_SNAPSHOT_HOLD_ALLOWED"})}
