import { createHash } from "node:crypto";
function obj(v){return !!v&&typeof v==="object"&&!Array.isArray(v)}
function canon(v){if(v===null)return"null";if(typeof v==="string"||typeof v==="boolean")return JSON.stringify(v);if(typeof v==="number"){if(!Number.isSafeInteger(v))throw new Error("P35_NON_SAFE_INTEGER");return JSON.stringify(v)}if(Array.isArray(v))return`[${v.map(canon).join(",")}]`;if(obj(v))return`{${Object.keys(v).sort().map(k=>`${JSON.stringify(k)}:${canon(v[k])}`).join(",")}}`;throw new Error("P35_UNSUPPORTED_CANONICAL_TYPE")}
const hash=v=>createHash("sha256").update(canon(v),"utf8").digest("hex");
export function compileProjectAdapterKitV1(input){if(!obj(input))throw new Error("PROJECT_ADAPTER_INPUT_REQUIRED");for(const k of["PROJECT_CONTRACT","PREFLIGHT_RULES","INPUT_POLICY","EXECUTOR_REQUIREMENTS","EVIDENCE_MAPPING","ACCEPTANCE_GATE","COST_POLICY","CREDENTIAL_REQUIREMENTS"])if(input[k]===undefined)throw new Error(`${k}_REQUIRED`);const body={SCHEMA_ID:"PROJECT_ADAPTER_KIT_V1",SCHEMA_VERSION:"1",PROJECT_ID:input.PROJECT_ID,PROJECT_CONTRACT:input.PROJECT_CONTRACT,PREFLIGHT_RULES:input.PREFLIGHT_RULES,INPUT_POLICY:input.INPUT_POLICY,EXECUTOR_REQUIREMENTS:input.EXECUTOR_REQUIREMENTS,EVIDENCE_MAPPING:input.EVIDENCE_MAPPING,ACCEPTANCE_GATE:input.ACCEPTANCE_GATE,COST_POLICY:input.COST_POLICY,CREDENTIAL_REQUIREMENTS:input.CREDENTIAL_REQUIREMENTS,RUNTIME_CORE_MUTATION_REQUIRED:false};return Object.freeze({...body,ADAPTER_SHA256:hash(body)})}
