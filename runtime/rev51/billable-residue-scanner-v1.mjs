import { createHash } from "node:crypto";
function obj(v){return !!v&&typeof v==="object"&&!Array.isArray(v)}
function canon(v){if(v===null)return"null";if(typeof v==="string"||typeof v==="boolean")return JSON.stringify(v);if(typeof v==="number"){if(!Number.isSafeInteger(v))throw new Error("P27_NON_SAFE_INTEGER");return JSON.stringify(v)}if(Array.isArray(v))return`[${v.map(canon).join(",")}]`;if(obj(v))return`{${Object.keys(v).sort().map(k=>`${JSON.stringify(k)}:${canon(v[k])}`).join(",")}}`;throw new Error("P27_UNSUPPORTED_CANONICAL_TYPE")}
const hash=v=>createHash("sha256").update(canon(v),"utf8").digest("hex");
const CLASSES=Object.freeze(["VM","SNAPSHOT","VOLUME","RESERVED_IP","LOAD_BALANCER","DATABASE","OBJECT_STORAGE","ARTIFACT_RETENTION","EGRESS_RESOURCE","PROVIDER_SPECIFIC"]);
function key(x){return`${x.CLASS}\0${x.RESOURCE_ID}`}
export function scanBillableResidueV1({expectedInventory,observedInventory}={}){if(!Array.isArray(expectedInventory)||!Array.isArray(observedInventory))throw new Error("BILLABLE_INVENTORY_REQUIRED");for(const x of [...expectedInventory,...observedInventory])if(!CLASSES.includes(x.CLASS)||typeof x.RESOURCE_ID!=="string"||!x.RESOURCE_ID)throw new Error("BILLABLE_RESOURCE_INVALID");const expected=new Set(expectedInventory.map(key)),observed=new Map(observedInventory.map(x=>[key(x),x]));const unexpected=[...observed].filter(([k])=>!expected.has(k)).map(([,v])=>v).sort((a,b)=>key(a).localeCompare(key(b)));const missing=expectedInventory.filter(x=>!observed.has(key(x)));const body={EXPECTED_COUNT:expectedInventory.length,OBSERVED_COUNT:observedInventory.length,UNEXPECTED_BILLABLE_RESIDUE:unexpected,MISSING_EXPECTED_RESOURCES:missing,JOB_CLOSE_ALLOWED:unexpected.length===0};return Object.freeze({...body,SCAN_SHA256:hash(body)})}
