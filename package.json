{
  "name": "@neoflowcore/atelier-runtime",
  "version": "1.0.0",
  "private": true,
  "description": "Thin deterministic Node/Web verification standard for GitHub Actions",
  "type": "module",
  "engines": {
    "node": "22.13.0"
  },
  "scripts": {
    "test": "node --test",
    "security:lint": "node scripts/security-lint.mjs",
    "release:gate": "node scripts/release-gate.mjs",
    "selftest": "npm run security:lint && npm test && npm run release:gate"
  }
}
