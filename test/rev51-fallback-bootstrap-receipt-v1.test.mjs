import test from"node:test";import assert from"node:assert/strict";import{buildFallbackBootstrapReceiptV1}from"../runtime/rev51/fallback-bootstrap-receipt-v1.mjs";const S="a".repeat(40),base={EXACT_CANDIDATE_SHA:S,REJECTED_PROVIDER:"sandbox",REJECTION_REASON:"RUNTIME_CAPABILITY_MISMATCH",SELECTED_PROVIDER:"github",EXECUTION_ADAPTER_STATE:"READY",SOURCE_TRANSPORT:"EXACT_SHA_CHECKOUT",EPHEMERAL_REF_CREATED:false,REMOTE_VALIDATION_RUNS:1,GENERATED_ARTIFACTS:["package-lock.json"],PROMOTION_STATE:"ELIGIBLE",CLEANUP_STATE:"PASS"};
test("P12 fallback receipt binds exact candidate and one-run budget",()=>assert.match(buildFallbackBootstrapReceiptV1(base).RECEIPT_SHA256,/^[0-9a-f]{64}$/));
test("P12 remote validation run budget exceeds one fails closed",()=>assert.throws(()=>buildFallbackBootstrapReceiptV1({...base,REMOTE_VALIDATION_RUNS:2}),/BUDGET_EXCEEDED/));
test("P12 ephemeral transport requires ref creation evidence",()=>assert.throws(()=>buildFallbackBootstrapReceiptV1({...base,SOURCE_TRANSPORT:"EPHEMERAL_VALIDATION_REF"}),/EVIDENCE_REQUIRED/));
test("P12 exact SHA transport rejects unexpected ephemeral ref",()=>assert.throws(()=>buildFallbackBootstrapReceiptV1({...base,EPHEMERAL_REF_CREATED:true}),/UNEXPECTED/));
