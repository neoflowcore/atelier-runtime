import test from"node:test";import assert from"node:assert/strict";import{evaluateSelfExecutionCapabilityV1,validateHumanHandoffV1}from"../runtime/rev51/self-execution-capability-v1.mjs";const req=["read_source","edit_source"];
test("P13 internal executor wins when it covers required operations",()=>{const r=evaluateSelfExecutionCapabilityV1({requiredOperations:req,executionPlanes:[{PLANE_ID:"RUNTIME_INTERNAL_EXECUTOR",STATE:"AUTO_EXECUTABLE",OPERATIONS:req},{PLANE_ID:"CONNECTED_PROVIDER_API",STATE:"AUTO_EXECUTABLE",OPERATIONS:req}]});assert.equal(r.decision,"AUTO_EXECUTABLE");assert.equal(r.planeId,"RUNTIME_INTERNAL_EXECUTOR")});
test("P13 automation path required is not human handoff",()=>{const r=evaluateSelfExecutionCapabilityV1({requiredOperations:req,executionPlanes:[{PLANE_ID:"DEVELOPER_WORKSPACE_MCP",STATE:"AUTOMATION_PATH_REQUIRED",OPERATIONS:req}]});assert.equal(r.decision,"AUTOMATION_PATH_REQUIRED");assert.equal(validateHumanHandoffV1({selfExecutionDecision:r,manualPlane:"SSH",explicitBreakGlassApproval:true,reason:"x"}).code,"HUMAN_HANDOFF_WITHOUT_SELF_EXECUTION_DENIAL")});
test("P13 missing automated capability becomes hard boundary",()=>assert.equal(evaluateSelfExecutionCapabilityV1({requiredOperations:req,executionPlanes:[]}).decision,"HARD_BOUNDARY"));
test("P13 hard boundary still requires explicit break glass approval",()=>{const h={decision:"HARD_BOUNDARY"};assert.equal(validateHumanHandoffV1({selfExecutionDecision:h,manualPlane:"SSH",reason:"no automated path"}).code,"BREAK_GLASS_APPROVAL_REQUIRED");assert.equal(validateHumanHandoffV1({selfExecutionDecision:h,manualPlane:"SSH",explicitBreakGlassApproval:true,reason:"no automated path"}).ok,true)});
