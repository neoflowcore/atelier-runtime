import test from"node:test";import assert from"node:assert/strict";import{classifyProviderOperationV1,authorityAllowsOperationV1,applyOperatorMutationTrustResetV1}from"../runtime/rev51/provider-effect-trust-reset-v1.mjs";
test("P16 POST query remains read-only by effect semantics",()=>{const d={OPERATION:"queryTelemetry",TRANSPORT_METHOD:"POST",EFFECT_CLASS:"QUERY",REQUIRED_CREDENTIAL_CAPABILITY:"OBS_READ"};const c=classifyProviderOperationV1(d);assert.equal(c.readOnly,true);assert.equal(c.providerMutation,false)});
test("P16 GET provider mutation is denied without mutation effect authority",()=>assert.equal(authorityAllowsOperationV1({descriptor:{OPERATION:"unsafeGet",TRANSPORT_METHOD:"GET",EFFECT_CLASS:"PROVIDER_MUTATION"},allowedEffectClasses:["PURE_READ","QUERY"]}).ok,false));
test("P16 operator mutation stales affected evidence only",()=>{const r=applyOperatorMutationTrustResetV1({evidence:[{DOMAIN:"deployment",STATUS:"PASS"},{DOMAIN:"source",STATUS:"PASS"}],mutation:{MUTATED:true,AFFECTED_DOMAINS:["deployment"]}});assert.equal(r.evidence[0].STATUS,"STALE");assert.equal(r.evidence[1].STATUS,"PASS")});
