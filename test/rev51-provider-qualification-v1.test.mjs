import test from"node:test";import assert from"node:assert/strict";import{buildProviderQualificationObservationV1,evaluateProviderQualificationV1}from"../runtime/rev51/provider-qualification-v1.mjs";const base={PROVIDER_ID:"p",WORKLOAD_ID:"w",ACTUAL_COST_MILLIUNITS:10,STARTUP_MS:100,EXECUTION_MS:1000,HUMAN_TOUCHES:0,CREDENTIAL_TOUCHES:0,AVAILABLE_DISK_MIB:1000,ARTIFACT_TRANSFER_MS:100,CLEANUP_STEPS:1,CLEANUP_FAILURES:0,ORPHAN_COUNT:0,UNKNOWN_OUTCOME_COUNT:0,RECONCILIATION_STEPS:0,CREDENTIAL_RECOVERY_STEPS:0,RESUME_SUPPORT:true,ANDROID_USABILITY:false,DUPLICATE_MUTATION_RISK:"LOW"},o=(x={})=>buildProviderQualificationObservationV1({...base,...x});
test("P30 observed provider metrics are content-bound",()=>assert.match(o().OBSERVATION_SHA256,/^[0-9a-f]{64}$/));
test("P30 qualification applies measured caps and required features",()=>assert.equal(evaluateProviderQualificationV1({observation:o(),policy:{MAX:{HUMAN_TOUCHES:0,ORPHAN_COUNT:0},REQUIRE_TRUE:["RESUME_SUPPORT"],ALLOWED:{DUPLICATE_MUTATION_RISK:["LOW"]}}}).qualified,true));
test("P30 excessive human touch fails qualification",()=>assert.equal(evaluateProviderQualificationV1({observation:o({HUMAN_TOUCHES:2}),policy:{MAX:{HUMAN_TOUCHES:0}}}).qualified,false));
test("P30 tampered observation digest fails closed",()=>assert.throws(()=>evaluateProviderQualificationV1({observation:{...o(),HUMAN_TOUCHES:2},policy:{MAX:{HUMAN_TOUCHES:0}}}),/SHA256_MISMATCH/));
