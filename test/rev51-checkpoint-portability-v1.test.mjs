import test from"node:test";import assert from"node:assert/strict";import{buildPortableCheckpointV1,compileCrossProviderResumeAttemptV1}from"../runtime/rev51/checkpoint-portability-v1.mjs";
test("P32 portable checkpoint binds source input boundary and artifacts",()=>assert.match(buildPortableCheckpointV1({JOB_ID:"j",SOURCE_IDENTITY:"git:a",INPUT_DIGEST:"d",COMPLETED_BOUNDARY:"step1",ARTIFACT_DIGESTS:["b","a"],STATE_PAYLOAD:{x:1}}).CHECKPOINT_SHA256,/^[0-9a-f]{64}$/));
test("P32 cross-provider resume uses new attempt lease and fence",()=>{const c=buildPortableCheckpointV1({JOB_ID:"j",SOURCE_IDENTITY:"git:a",INPUT_DIGEST:"d",COMPLETED_BOUNDARY:"step1"});const r=compileCrossProviderResumeAttemptV1({checkpoint:c,targetProvider:"p2",newAttemptId:"a2",newLeaseId:"l2",newFenceId:"f2"});assert.equal(r.PROCESS_MIGRATION,false);assert.equal(r.RESUME_MODE,"DURABLE_BOUNDARY")});
test("P32 tampered checkpoint is rejected before resume",()=>{const c=buildPortableCheckpointV1({JOB_ID:"j",SOURCE_IDENTITY:"git:a",INPUT_DIGEST:"d",COMPLETED_BOUNDARY:"step1"});assert.throws(()=>compileCrossProviderResumeAttemptV1({checkpoint:{...c,INPUT_DIGEST:"x"},targetProvider:"p2",newAttemptId:"a2",newLeaseId:"l2",newFenceId:"f2"}),/SHA256_MISMATCH/)});
