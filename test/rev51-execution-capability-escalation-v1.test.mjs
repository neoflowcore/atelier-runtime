import test from"node:test";import assert from"node:assert/strict";import{selectExecutionPlaneV1,validateManualEscalationV1}from"../runtime/rev51/execution-capability-escalation-v1.mjs";const req=["read"];
test("P14 canonical escalation prefers connected API before remote runner",()=>{const r=selectExecutionPlaneV1({requiredOperations:req,planes:[{PLANE_ID:"APPROVED_REMOTE_RUNNER",AVAILABLE:true,OPERATIONS:req},{PLANE_ID:"CONNECTED_PROVIDER_API",AVAILABLE:true,OPERATIONS:req}]});assert.equal(r.selected,"CONNECTED_PROVIDER_API")});
test("P14 manual is denied while automated path exists",()=>{const r=validateManualEscalationV1({requiredOperations:req,planes:[{PLANE_ID:"CONNECTED_PROVIDER_API",AVAILABLE:true,OPERATIONS:req},{PLANE_ID:"BREAK_GLASS_MANUAL",AVAILABLE:true,OPERATIONS:req}],breakGlassApproval:true});assert.equal(r.code,"AUTOMATED_PATH_EXISTS_MANUAL_DENIED")});
test("P14 break glass requires approval when no automation exists",()=>assert.equal(selectExecutionPlaneV1({requiredOperations:req,planes:[{PLANE_ID:"BREAK_GLASS_MANUAL",AVAILABLE:true,OPERATIONS:req}],breakGlassApproval:false}).code,"BREAK_GLASS_APPROVAL_REQUIRED"));
test("P14 approved break glass is last resort",()=>assert.equal(validateManualEscalationV1({requiredOperations:req,planes:[{PLANE_ID:"BREAK_GLASS_MANUAL",AVAILABLE:true,OPERATIONS:req}],breakGlassApproval:true}).code,"BREAK_GLASS_SELECTED"));
