# Runtime Rev5.1 P5A — End-to-End Trace Context

A Runtime trace binds phase, job, attempt, operation, credential lease, provider resource, evidence, and receipt identities. External source/provider/destructive mutations require a trace and an operation identity; traceless external mutation is denied.
